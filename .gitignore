# Python
__pycache__/
*.pyc

# OS
.DS_Store

# Large / external raw NGS reads — deposit in a sequence archive (e.g. SRA/ENA),
# do not commit. Only the small derived summaries under results/ are tracked.
*.fastq
*.fastq.gz
*.collapsed.csv
!examples/demo.collapsed.csv

# Bulky per-read processed outputs regenerated by the pipeline (keep out of git;
# the aggregate results/QC_summary.csv and the examples/ demo are enough to reproduce).
*.translated_filtered.csv
*.cdr_collapsed.csv
!examples/demo.translated_filtered.csv
!examples/demo.cdr_collapsed.csv
